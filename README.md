TypeScript ExtJS Store Remote Data Demo
=================================================

使用Store从远程url中读取数据供使用。

```
npm install
npm run server
npm start
```

It will open page on browser automatically.
